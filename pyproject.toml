[project]
name = "omnitransform"
version = "0.1.0"
description = "Portable AI skill: one input, any output."
requires-python = ">=3.10"
license = { file = "LICENSE" }
readme = "README.md"

[tool.omnitransform]
skill = "skills/omnitransform"
